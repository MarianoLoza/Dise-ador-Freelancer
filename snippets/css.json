{
	// Place your snippets for css here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }

    "Crea un Media Querie":{
		"prefix" : "mq", /* prefix es como se va a usar este atajo (poner mq en el css) */
		"body" : "@media (min-width: $1) {\n    $2\n}" /* el signo de $1 lo que hace es que cuando utilice el media querie ("mq" linea anterior) va a poner el cursor en $1 y se presiona tab va a poner el cursor en $2 para empezar a escribir el selector */
	}

}


/*para abrir esto poner en el css ctrl + shift + p y escribir "user snippets" (se pueden crear snippets para cualquier lenguaje que soporte studio code) y buscar "css.json, lo abris y salta la gilada. */
/*esto se usa para no llenar todo de codigo quearie el css*/